# [Claude Code 투입용 프롬프트] AI 개념 학습 웹게임 제작

> 아래 전체를 Claude Code에 그대로 붙여넣으세요.
> 새 빈 폴더에서 `claude` 실행 → 붙여넣기 → 1단계(스펙) 확인 후 진행.

---

## 0. 역할과 최종 목표

너는 이 프로젝트의 리드 개발자이자 교육 콘텐츠 설계자다.

**만들 것:** AI를 전혀 모르는 일반 직장인이 **15~20분 안에 플레이하면서** "AI가 실제로 어떻게 일하는지"와 "AI 도구들이 서로 뭐가 다른지"를 몸으로 이해하게 만드는 **웹 브라우저 게임**.

**형식:** 하나의 게임이 아니라 **미니게임 모음(연수원 코스 맵)**. 개념마다 그 개념을 설명하기 가장 좋은 게임 방식이 따로 있다. 같은 조작을 반복시키면 지루해서 이탈한다.

**목적:** 사내 AX(AI Transformation) 확산 교육용. 자료를 읽히는 대신 직접 조작하고 실패해보게 만든다.

**성공 기준:**
- 비개발자가 설명서 없이 30초 안에 뭘 하는 건지 이해한다
- 플레이 후 "스킬이랑 서브에이전트가 뭐가 다른데?", "프로젝트랑 아티팩트가 뭔데?"에 한 문장으로 답할 수 있다
- **"Claude Code는 개발자만 쓰는 거 아니야?"라는 오해가 깨진다**
- 실패했을 때 왜 실패했는지가 결과 화면에서 즉시 이해된다
- **"AI = ChatGPT" 인식이 깨지고, 일에 따라 다른 도구를 찾아볼 생각이 생긴다**
- 링크 하나 보내면 모바일에서 바로 열린다

---

## 1. 대상 사용자

- AI를 "ChatGPT에 질문하는 것" 정도로만 아는 사무직
- 개발 지식 없음, 영어 용어에 거부감 있음
- 모바일로 열어볼 확률이 높음 (사내 메신저 링크 공유)
- 교육을 강요당하는 입장이라 **지루하면 바로 이탈함**

→ 한국어 UI. 영문 용어는 `스킬(Skills)` 형태로 병기. 비유 먼저, 정의 나중.

---

## 2. 기술 제약 (반드시 지킬 것)

| 항목 | 요구사항 |
|---|---|
| 스택 | 순수 HTML + CSS + JavaScript (ES Modules). 빌드 단계 없음 |
| 프레임워크 | React/Vue/번들러 전부 금지. `npm install` 없이 `index.html`로 실행 |
| 외부 의존성 | **CDN 포함 전면 금지.** 폰트·아이콘·라이브러리 전부 로컬 또는 시스템 폰트·인라인 SVG (사내망 차단 대비) |
| LLM API | **호출 금지.** 모든 판정은 자체 룰 엔진으로. API 키 없이 영구 무료 동작 |
| 저장 | `localStorage`로 진행도 저장 + "처음부터" 초기화 버튼 |
| 배포 | GitHub Pages. 루트 배포, `.nojekyll` 포함, **전부 상대경로**(`./assets/`) |
| 반응형 | 모바일 우선(360px 기준) → 데스크톱 확장. 터치 타겟 44px 이상 |
| 입력 | **모바일 드래그가 되는지 반드시 확인.** 안 되면 탭-투-탭(집었다 놓기)으로 대체 |
| 접근성 | 키보드 조작 가능, 포커스 링 유지, `prefers-reduced-motion` 존중 |
| 개인정보 | 로그인·수집·애널리틱스 전부 없음 |
| 파일 크기 | 파일 하나가 500줄 넘어가면 분리 |

---

## 3. 전체 구조: 연수원 코스 맵

```
[코스 맵]  ●──●──●──◐──○──○──○     ← 노드 하나 = 미니게임 하나 (2분 이내)
            │  │  │
            └──┴──┴─ 클리어하면 용어 카드 해금 → [용어 도감]에 수집

  챕터 1        챕터 2         챕터 3        챕터 4       챕터 5      [번외]
  AI 기본기  →  도구 구분  →  일 시키기  →  안전장치  →  실전 종합    AI 도구 도감
                                                                  (언제든 열람)
```

- 노드 하나 = **2분 이내**. 지하철에서 한 판 하고 끊을 수 있어야 함
- 언제든 중단·재개 가능
- **용어 도감**은 게임 안 해도 사전처럼 열람 가능 (교육 후 레퍼런스로 계속 쓰임)
- 랭크 승급: 관람객 → 프롬프터 → 컨텍스트 설계자 → 하네스 엔지니어
- 마지막에 결과 카드(획득 용어 수 + 성향) — 캡처해서 공유하기 좋게

---

## 4. ★ 가장 중요한 설계 원칙: 5개 엔진 × 데이터 = 17개 미니게임

미니게임을 17개 따로 만들면 코드가 무너진다. **재사용 가능한 게임 템플릿 5종**을 만들고, 각 미니게임은 **템플릿 + 데이터 파일**로만 구성해라.

| 엔진 | 조작 방식 | 어떤 개념에 쓰나 |
|---|---|---|
| **A. 분류형** | 카드가 나오면 올바른 통에 넣기 | 서로 헷갈리는 것들을 구분시킬 때 |
| **B. 판별형** | 여러 개 중 골라내기 / 숨은 위험 찾기 | 진짜 vs 가짜, 위험 신호 찾기 |
| **C. 조립형** | 슬롯에 부품 배치 / 포트 연결 | 구성 요소를 조합해서 결과 만들기 |
| **D. 배치형** | 타임라인에 순서·병렬 배치 | 흐름·순서·위임 구조 |
| **E. 미리보기형** | 조작하면 결과 텍스트가 즉시 바뀜 | 입력이 출력을 어떻게 바꾸는지 |

각 엔진은 공통 인터페이스를 갖는다:

```js
// 모든 미니게임이 이 형태를 따른다
export default {
  id: 'hallucination-spotter',
  engine: 'B',              // 판별형
  title: '지어낸 답 찾기',
  concept: ['할루시네이션'], // 클리어 시 해금할 용어 카드
  timeLimit: 90,
  data: { /* 엔진이 해석하는 문제 데이터 */ },
  debrief: '왜 이런 일이 생기나 — 3줄'
}
```

새 개념을 추가할 때 **데이터 파일 하나만 추가하면 되게** 만들어라. 이게 이 프로젝트의 유지보수 핵심이다.

---

## 5. 미니게임 목록

아래는 **개념 → 게임 방식** 매핑이다. 방식은 개선해도 되지만, "왜 이 개념엔 이 조작인가"를 반드시 정당화해라.

### 챕터 1 — AI 기본기

| # | 미니게임 | 엔진 | 배우는 것 |
|---|---|---|---|
| 1 | **가방 싸기** — 제한된 칸에 정보 조각을 넣는다. 넘치면 먼저 넣은 게 회색으로 밀려나감 | C | 토큰, 컨텍스트 윈도우, 컨텍스트 로트 |
| 2 | **지어낸 답 찾기** — AI 답변 3개 중 없는 사실을 지어낸 것 골라내기. 근거 문서와 대조 | B | 할루시네이션(환각), 근거 확인 습관 |
| 3 | **지시문 고쳐쓰기** — 나쁜 프롬프트에 `역할`·`맥락`·`형식`·`예시` 태그를 붙이면 **오른쪽 결과물이 실시간으로 바뀐다** | E | 프롬프트 엔지니어링의 4요소 |
| 4 | **자료 찾아오기** — 질문이 오면 서가에서 관련 문서만 집어와 AI에게 건넴. 엉뚱한 걸 주면 엉뚱한 답이 나옴 | B | RAG, 검색 기반 답변 |

### 챕터 2 — 도구 구분 (★ 이 챕터가 이 게임의 차별점)

| # | 미니게임 | 엔진 | 배우는 것 |
|---|---|---|---|
| 5 | **어디서 할까?** — 업무 상황 카드를 보고 적절한 도구를 고른다. "이 대화 계속 이어서 할 자료들을 모아두고 싶다"→프로젝트 / "결과물을 문서로 뽑아 계속 고치고 싶다"→아티팩트 / "내 PC 파일 200개를 정리하고 싶다"→Claude Code | A | **프로젝트, 아티팩트, 채팅, Claude Code, Cowork의 차이** |
| 6 | **모델이냐 제품이냐** — 이름 카드를 `모델` / `제품(도구)` 두 통에 분류. GPT-5.x·Claude Opus·Gemini는 모델, ChatGPT·Claude.ai·Codex·Claude Code는 제품 | A | 가장 흔한 혼동 정리 |
| 7 | **이름에 속지 마라** — "Claude Code의 Code는 무슨 뜻일까?" 오해 카드를 하나씩 깨는 방식. 실제로 비개발자가 쓰는 사례(파일 정리, 엑셀 변환, 문서 일괄 수정)를 보여줌 | B | **Claude Code / GPT Codex의 정체와 이름의 함정** |
| 8 | **도구함 정렬** — 상황 카드가 내려오면 `커맨드`/`스킬`/`서브에이전트`/`훅`/`플러그인`/`MCP` 중 맞는 도구함에 분류. 오답 시 즉시 한 줄 힌트 | A | 제일 헷갈리는 5형제 구분 (반복 학습형) |
| 9 | **배선 연결** — AI와 외부 시스템(메일·캘린더·사내DB) 포트를 케이블로 잇는다. 권한 스위치도 함께 — 다 열면 나중에 사고남 | C | MCP, 커넥터, 최소 권한 |

### 챕터 3 — 일 시키기

| # | 미니게임 | 엔진 | 배우는 것 |
|---|---|---|---|
| 10 | **신입 온보딩 문서 만들기** — 처음 온 AI에게 줄 지침서에 규칙 카드를 골라 넣는다. 너무 많이 넣으면 오히려 성능이 떨어짐 | C | CLAUDE.md / AGENTS.md, 컨텍스트 엔지니어링 |
| 11 | **팀 배치** — 일감을 타임라인에 순차/병렬로 배치. 병렬은 빠르지만 결과 합치기 실패가 생김 | D | 서브에이전트, 멀티 에이전트, 오케스트레이션(코디네이터·팬아웃·파이프라인) |
| 12 | **자동화 라인** — 반복 업무를 워크플로우로 조립. 사람 검토 지점을 어디에 둘지 결정 | D | 에이전트 루프, 휴먼 인 더 루프, 자동화 설계 |

### 챕터 4 — 안전장치

| # | 미니게임 | 엔진 | 배우는 것 |
|---|---|---|---|
| 13 | **사고 막기** — 에이전트가 자동으로 일하는 걸 지켜보다가 위험 지점에 훅·검증·샌드박스를 배치. 늦으면 사고 발생 | D | 훅(Hooks), 하네스 엔지니어링, 가드레일, 샌드박스 |
| 14 | **결재 반려** — 쏟아지는 AI 산출물 중 그럴듯한데 알맹이 없는 것을 반려. 시간 압박 있음 | B | 워크슬롭, AI 슬롭, 검증 부채, AI 번아웃 |
| 15 | **이상 징후 찾기** — 사무실 화면에서 위험한 행동 클릭 (외부 사이트 지시를 그대로 따르는 AI, 승인 안 된 도구에 사내 문서 업로드 등) | B | 프롬프트 인젝션, 섀도우 AI, 정보 유출, 자동화 편향 |

### 챕터 5 — 실전 종합

| # | 미니게임 | 엔진 | 배우는 것 |
|---|---|---|---|
| 16 | **업무 티켓 처리** — 실제 업무 요청 3건을 받아 지금까지 배운 부품을 자유롭게 조합해 해결. 판정: 성공/부분성공/실패(환각·워크슬롭·컨텍스트 초과·보안사고) | C | 전체 종합 |

### 번외 — AI 도구 도감

| # | 미니게임 | 엔진 | 배우는 것 |
|---|---|---|---|
| 17 | **이 일엔 어떤 AI?** — 업무 상황 카드("해외 경쟁사 20곳 조사해서 표로", "제품 홍보 영상 15초", "회의록 정리")를 보고 **도구 이름이 아니라 카테고리**를 고른다. 고르면 그 카테고리의 도구 카드들이 펼쳐짐 | A | 용도별 도구 지형도 |

+ **AI 도구 도감**(게임 아님, 상시 열람 가능한 카탈로그) — 상세 요구사항은 아래 6-B

---

## 6. ★ 제품·도구 구분 콘텐츠 (챕터 2용 상세 요구사항)

여기가 사람들이 제일 헷갈리는데 아무도 안 알려주는 영역이다. **정확도가 생명이니 반드시 공식 문서로 검증해라.**

### 반드시 다뤄야 할 구분

**(1) 모델 vs 제품 vs 표면(surface)**
- 모델 = 엔진 / 제품 = 그 엔진으로 만든 도구 / 표면 = 그 도구를 쓰는 창구(웹·앱·터미널·IDE·오피스)
- 같은 모델이 여러 표면에서 돌아간다는 점을 이해시킬 것

**(2) Claude 계열 — 각각 언제 쓰는지 한 문장씩**
- 일반 채팅
- **프로젝트(Projects)** — 관련 자료와 지침을 묶어두는 작업 공간. 대화마다 배경 설명을 다시 안 해도 됨
- **아티팩트(Artifacts)** — 대화 옆에 만들어지는 실물 산출물(문서·표·코드·작동하는 웹앱). 대화가 아니라 **결과물**이라는 점이 핵심
- **스킬(Skills)** — 반복 작업의 방법을 담아둔 지침 묶음. 필요할 때 자동으로 불려나옴
- **커넥터 / MCP** — 외부 시스템과 연결하는 표준 규격
- **Claude Code** — 터미널·IDE·데스크톱에서 도는 에이전틱 도구
- **Cowork** — 비개발자용 에이전틱 업무 앱
- 오피스 연동(Excel·PowerPoint·Word·Chrome 등) — 있는 것만 정확히

**(3) "Claude Code의 Code가 뭐냐" — 미니게임 7의 핵심**
- 코딩을 위해 만들어졌지만 본질은 **"내 컴퓨터의 파일과 명령을 직접 다루는 에이전트"**
- 그래서 비개발자도 문서 일괄 변환·파일 정리·데이터 가공에 쓴다
- **이름 때문에 생기는 진입장벽 자체가 학습 포인트다.** 이걸 명시적으로 깨라

**(4) GPT / OpenAI 계열**
- ChatGPT(제품) vs GPT-5.x(모델) 구분
- **Codex**: 2021년의 코드 자동완성 모델과 **지금의 Codex는 다른 것**. 현재는 ChatGPT에 붙은 에이전트 제품으로, 클라우드에서 비동기 실행 + 로컬 CLI + IDE 확장 형태. 대략 Claude Code에 대응하는 위치
- 설정 파일: **AGENTS.md**(Codex 계열) vs **CLAUDE.md**(Claude Code). 목적은 같고 이름이 다르다는 점

**(5) 오해 깨기 카드 (게임 안에 "흔한 오해" 형태로 넣을 것)**
- "아티팩트 = 파일 첨부?" → 아니다
- "프로젝트 = 그냥 폴더?" → 아니다
- "Codex = 옛날 코딩 모델?" → 아니다
- "Claude Code = 개발자 전용?" → 아니다
- "스킬 = 플러그인?" → 아니다

### 검증 지시 (필수)

제품 기능은 **몇 달 단위로 바뀐다.** 아래를 반드시 확인하고, 확인 안 된 건 넣지 마라.
- https://docs.claude.com/en/docs/claude-code/overview (Claude Code)
- https://support.claude.com (Claude.ai 기능 — 프로젝트·아티팩트 등)
- https://docs.claude.com/en/docs_site_map.md (전체 문서 맵)
- OpenAI / Codex는 공식 문서·릴리즈 노트로 확인

**콘텐츠 데이터마다 `checkedAt: '2026-08'` 필드를 넣어라.** 나중에 뭘 다시 확인해야 하는지 알 수 있게.

---

## 6-B. ★ 번외: AI 도구 도감

"AI 하면 ChatGPT밖에 모른다"를 깨는 파트. 게임 클리어와 무관하게 **언제든 열람 가능한 카탈로그**로 만들고, 미니게임 17에서 자연스럽게 진입시킨다.

### 설계 원칙 (이거 안 지키면 반년 만에 쓰레기가 된다)

1. **카테고리가 뼈대, 도구는 예시다.** 개별 도구는 6개월이면 바뀌거나 사라진다. "영상 생성"이라는 칸은 남고 그 안의 이름만 갈리도록 구조를 짜라
2. **순위·최고 표현 금지.** "가장 좋은 AI"가 아니라 "이 일에는 이 계열"로만 서술
3. **가격·성능 수치 넣지 마라.** 몇 주 단위로 바뀐다. `무료 / 부분 무료 / 유료` 정도의 거친 구분만
4. **벤더 마케팅 문구 복붙 금지.** 각 도구 소개는 그 회사 홍보 문구가 아니라 "무슨 일에 쓰는 물건인지"로 다시 써라
5. **모든 카드에 `checkedAt: '2026-08'` 와 공식 링크 필수**
6. 서비스가 통합·종료되는 경우가 자주 있으니(영상 생성 쪽이 특히 심함) **넣기 전에 현재도 서비스 중인지 확인**

### 도구 카드 필드

```js
{
  name: 'Manus',
  category: '자율 에이전트',
  oneLine: '목표만 주면 알아서 계획 세우고 끝까지 해오는 에이전트',
  goodAt: ['긴 조사 작업', '결과물(문서·표) 완성까지'],
  watchOut: ['중간에 방향이 틀리면 끝까지 틀린 채로 완성됨 → 검토 필수'],
  useWhen: '사람이 몇 시간 붙어야 할 조사·정리 작업',
  pricing: '부분 무료',
  official: 'https://...',
  checkedAt: '2026-08'
}
```

`watchOut`(못하는 것·주의점)을 **반드시 채워라.** 장점만 나열하면 그 자체가 워크슬롭이고, 챕터 4에서 배운 내용과 모순된다.

### 다룰 카테고리와 도구 (조사해서 확정할 것)

| 카테고리 | 포함할 도구 (예시 — 검증 후 조정) |
|---|---|
| 범용 채팅 | ChatGPT, Claude, Gemini, Grok, Microsoft Copilot, 국내 서비스(뤼튼 등 — 현황 확인) |
| 리서치·검색 | Perplexity, 각 사 딥리서치 기능, NotebookLM |
| 자율 에이전트 | Manus, Claude Cowork, 각 사 에이전트 모드 |
| 코딩 에이전트 | Claude Code, Codex, Cursor, GitHub Copilot, Gemini CLI |
| 영상 생성 | Higgsfield, Veo, Kling, Seedance, Runway, Luma, Pika |
| 아바타·발표 영상 | HeyGen, Synthesia (위와 **다른 카테고리**임을 명시) |
| 이미지 생성 | Midjourney, Gemini 이미지(나노바나나 계열), GPT 이미지, Flux, Stable Diffusion |
| 음성·더빙 | ElevenLabs 등 |
| 문서·오피스 | Notion AI, Gamma, Copilot 365, Claude의 오피스 연동 |
| 업무 자동화 | n8n, Make, Zapier, Dify |
| 로컬 실행 | Ollama, LM Studio (사내 자료를 밖으로 안 보내는 선택지가 있다는 걸 알리는 용도) |

### 반드시 넣을 개념 하나: **애그리게이터**

Higgsfield처럼 **여러 모델을 하나로 묶어 파는 층**이 있다는 걸 설명해라. 챕터 2의 "모델 vs 제품" 구분이 여기서 한 번 더 쓰인다 — Higgsfield는 모델이 아니라 Veo·Kling 같은 여러 엔진을 골라 쓰게 해주는 **플랫폼**이다. 이 구조를 이해하면 "AI 툴이 수백 개"라는 혼란이 정리된다.

### ★ 도감 상단 고정 배너 (필수)

> **여기 나온 도구를 회사 업무에 바로 쓸 수 있다는 뜻이 아닙니다. 사내 자료를 넣기 전에 승인된 도구인지 반드시 확인하세요.**

챕터 4에서 섀도우 AI를 가르쳐놓고 도감이 미승인 도구 사용을 부추기면 앞뒤가 안 맞는다. 배너는 스크롤해도 사라지지 않게 하고, 미니게임 15(이상 징후 찾기)와 상호 링크해라.

---

## 7. 그 외 다뤄야 할 용어

각 용어는 `한 줄 비유 / 3줄 설명 / 실무 예시 / 자주 하는 오해` 4필드로.

**LLM 기본:** 토큰, 컨텍스트 윈도우, 시스템 프롬프트, 할루시네이션, RAG, 임베딩·벡터DB, 파인튜닝 vs 프롬프팅, 멀티모달, 추론(reasoning) 모델

**도구 계층:** CLAUDE.md·AGENTS.md, 슬래시 커맨드, 스킬(SKILL.md), 서브에이전트, 훅, 플러그인, MCP, 컨텍스트 압축, 체크포인트

**방법론 진화 (챕터 진행의 뼈대):** 프롬프트 엔지니어링 → 컨텍스트 엔지니어링(2025, Karpathy 계기) → **하네스 엔지니어링**(2026.02, Mitchell Hashimoto가 명명 → OpenAI Codex 리포트 → Anthropic 장기실행 에이전트 하네스 글로 확산)

**에이전트/워크플로우:** 에이전트 루프, 툴 콜링, 멀티 에이전트, 오케스트레이션 패턴, 컨텍스트 격리, 휴먼 인 더 루프, 가드레일, 샌드박스, 바이브 코딩, 스펙 주도 개발

**2026년 현장 용어:** 워크슬롭, AI 번아웃·AI 피로, 컨텍스트 로트, AI 슬롭, 섀도우 AI, 프롬프트 인젝션, 자동화 편향, 검증 부채

### 웹 검색 지시

WebSearch로 **2026년 8월 기준** 확인하고:
1. 정의가 현재도 맞는지 검증 (도구 계층·현장 용어는 변화가 빠름)
2. 목록에 없는데 실무에서 자주 쓰이는 용어 5~10개 추가 발굴
3. **출처가 불확실하면 넣지 마라**
4. 결과는 `TERMS.md`로 정리(출처 링크 포함) — 콘텐츠 수정은 여기서만 하게

---

## 8. 디자인 방향

**금지:** AI가 만든 티가 나는 기본값 — 크림색 배경 + 세리프 헤드라인 + 테라코타 포인트, 새까만 배경 + 형광 그린, 이모지 남발, 의미 없는 그라데이션.

**방향:** 소재는 "연수원 코스 + 작업 지시 + 실행 로그"다. **작업 티켓·공정 파이프라인·계기판**의 시각 언어에서 출발하되, 개발자용 검정 터미널처럼 보이면 안 된다(비개발자 대상).

- 시그니처 요소 하나만 정해 거기에 힘을 몰아라
- **미니게임 17개가 제각각으로 보이면 안 된다.** 공통 셸(상단바·진행도·해설 패널)을 먼저 고정하고, 각 게임은 그 안에서만 달라져라
- 타이포: 본문은 한글 가독성 우선(시스템 폰트 스택), 데이터·로그는 고정폭
- 색 4~6개, 각각 역할 지정(성공/부분성공/실패/중립)
- 애니메이션은 "AI가 일하는 중"과 "실패하는 순간"에만

코드 짜기 전에 팔레트·타이포·레이아웃·시그니처를 정한 **디자인 토큰 표를 먼저** 만들고, "아무 프로젝트에나 쓸 법한 기본값"인지 스스로 검토한 뒤 진행.

---

## 9. 파일 구조 (제안)

```
/
├── index.html
├── .nojekyll
├── README.md              # 게임 소개 + 로컬 실행 + Pages 배포 방법
├── TERMS.md               # 용어 원본(출처 포함) — 콘텐츠 수정은 여기서
├── GAME_SPEC.md           # 1단계 산출물
├── assets/styles.css
└── src/
    ├── main.js
    ├── shell/             # 공통 셸: 코스맵, 상단바, 해설 패널, 도감
    │   ├── coursemap.js
    │   ├── debrief.js
    │   ├── codex.js       # 용어 도감
    │   └── toolbook.js    # 번외: AI 도구 도감(카탈로그)
    ├── engines/           # ★ 게임 템플릿 5종
    │   ├── sort.js        # A 분류형
    │   ├── spot.js        # B 판별형
    │   ├── build.js       # C 조립형
    │   ├── timeline.js    # D 배치형
    │   └── preview.js     # E 미리보기형
    ├── games/             # 미니게임 = 엔진 지정 + 데이터 (파일당 하나)
    │   ├── 01-context-bag.js
    │   ├── 02-hallucination.js
    │   └── ...
    ├── data/
    │   ├── terms.js       # 용어 도감
    │   ├── products.js    # 제품·도구 구분 데이터 (checkedAt 포함)
    │   └── tools.js       # 번외: AI 도구 카탈로그 (checkedAt·공식링크 필수)
    └── core/
        ├── state.js       # 진행도 + localStorage
        └── router.js
```

**엔진과 데이터를 반드시 분리해라.** 미니게임 하나 추가가 `games/` 파일 하나 추가로 끝나야 한다.

---

## 10. 작업 순서

**1단계 — 스펙 먼저 (여기서 멈추고 확인받아라)**
`GAME_SPEC.md`: 엔진 5종의 인터페이스 정의, 미니게임 17개 각각의 데이터 스키마·정답·오답 피드백, 용어 해금 매핑, 디자인 토큰. **코드는 아직 금지.**

**2단계 — 콘텐츠 조사**
용어 검증·보강 + **제품 구분 정보 공식 문서로 검증** → `TERMS.md`, `data/products.js`
번외 도구 도감은 **카테고리 틀부터 확정한 뒤** 도구를 채워라. 서비스 중단·통합 여부까지 확인 → `data/tools.js`

**3단계 — 셸 + 엔진 1개**
공통 셸 + 엔진 A(분류형) + 미니게임 1개만 완성해서 **끝까지 도는 걸 먼저 보여줘라.** 여기서 내 피드백 받고 나머지 진행

**4단계 — 나머지 엔진 4종 + 미니게임 16개 + 도구 도감**

**5단계 — 자체 검수**
- 미니게임 17개 전부 **정답 경로 / 오답 경로 두 번** 플레이 워크스루하고 보고
- 콘솔 에러 0 / 360px 폭 정상 / 모바일 드래그 동작 / localStorage 초기화 후 정상
- **외부 네트워크 요청 0건** 확인
- 처음부터 끝까지 실제 소요 시간 측정 (20분 넘으면 줄여라)

**6단계 — 배포 준비**
`README.md`에 GitHub Pages 배포 절차(Settings → Pages → Deploy from branch → main / root)와 접속 URL 형식.

각 단계 끝날 때 **한 줄 요약 + 다음 단계**만 보고하고 넘어가라.

---

## 11. 하지 말 것

- 전부 4지선다 퀴즈로 만들기 (조작 방식이 개념마다 달라야 한다는 게 이 프로젝트의 핵심)
- 반대로 미니게임 17개를 서로 다른 코드로 따로 짜기 (엔진 5종으로 묶어라)
- 개발자 전용 표현이 게임 화면에 등장 ("레포 클론", "npm run dev")
- 한 화면에 설명 3줄 초과
- **검증 안 된 제품 정보를 그럴듯하게 지어내기** — 이 게임 자체가 워크슬롭이 되면 안 된다
- 도구 도감에 가격·벤치마크 점수·순위 넣기 (몇 주 만에 틀린 정보가 된다)
- 도구 소개를 벤더 홍보 문구로 채우기, 단점 칸 비워두기
- 특정 회사·조직명, 사내 시스템명 하드코딩 (공개 저장소에 올라감)
- 사용자 데이터 수집, 외부 요청, 로그인

---

## 12. 완료 체크리스트

- [ ] `index.html`만으로 전체 플레이 가능, 외부 네트워크 요청 0건
- [ ] 미니게임 17개가 엔진 5종 위에 데이터로 구현됨
- [ ] 모바일 360px + 터치 조작 정상
- [ ] 제품 구분(프로젝트/아티팩트/Claude Code/Codex/모델 vs 제품) 콘텐츠가 공식 문서로 검증되고 `checkedAt` 기록됨
- [ ] 용어 35개 이상, 각 4필드
- [ ] 도감을 게임 밖에서 사전처럼 열람 가능
- [ ] 진행도 저장/재개/초기화 동작
- [ ] AI 도구 도감: 카테고리 10개 내외, 각 도구에 단점·`checkedAt`·공식 링크 채워짐
- [ ] 도감 상단에 "사내 승인 확인" 배너 고정
- [ ] 전체 플레이 20분 이내 (도감 열람 시간 제외)
- [ ] `games/` 파일 하나 추가로 미니게임 확장 가능한 구조

---

**시작해라. 1단계 `GAME_SPEC.md`부터.**
