# CLAUDE.md — 이 저장소의 작업 규칙

> 프로젝트: **AI 연수원** (사내 AX 확산 교육용 웹게임)
> 세션 시작 시 이 파일과 `PROGRESS.md` 를 먼저 읽는다.
> 사용자 지시와 충돌할 때만 사용자 지시가 우선한다.

---

## 0. 전제 (규칙의 근거)

- 사용자는 **코드를 읽지 못한다.** → 모든 변경은 **화면 또는 수치로 증명**한다. "고쳤습니다"는 증거가 아니다.
- 리뷰어가 없다. → **이 규칙이 리뷰어를 대신한다.**
- 결과물이 **공개 저장소**에 올라가고 **사내 교육**에 쓰인다. → 틀린 정보 하나가 교육 전체의 신뢰를 깎는다.

---

## 1. 절대 규칙

1. **검증 안 된 제품 정보를 쓰지 않는다.**
   챕터 2 콘텐츠는 공식 문서로 확인한 것만 넣는다. 확인 못 하면 **빼고 `TERMS.md` 부록 B에 사유를 적는다.**
   이 게임 자체가 워크슬롭이 되면 안 된다 — 그게 챕터 4에서 가르치는 내용이다.

2. **콘텐츠 원본은 `TERMS.md`.** 용어 문구를 고칠 때 `src/data/terms.js` 를 먼저 고치지 않는다.
   `TERMS.md` → `data/*.js` 방향으로만 옮긴다.

3. **엔진에 콘텐츠를 넣지 않는다.**
   `engines/` 는 특정 미니게임을 몰라야 한다. 문항·정답·오답 문구·해설은 전부 `games/` 와 `data/`.

4. **색·크기·간격은 `assets/tokens.css` 에만 존재한다.** 다른 파일에 `#`으로 시작하는 색값을 쓰지 않는다.

5. **외부 요청 0건을 유지한다.** 금지되는 것은 **다른 서버로 나가는 요청**이다 —
   CDN·애널리틱스·API 호출·바깥 이미지. 사내망에서 차단되거나 느려지면 교육이 멈춘다.
   반대로 **저장소 안에 넣어 같은 출처에서 부르는 자산은 된다** (`assets/vendor/`):
   글꼴 Pretendard 조각, 애니메이션 라이브러리가 그렇게 들어 있다.
   자산을 넣을 때는 ① 라이선스 원문을 같은 폴더에, ② 경로는 상대경로로,
   ③ 없어도 화면이 읽히게(글꼴은 시스템 글꼴로 대체) 만든다.
   추가 후에는 개발자도구 Network 탭에서 **바깥으로 나가는 요청이 0건인지 세어 본다.**

6. **모든 경로는 상대경로.** `./assets/` 는 되고 `/assets/` 는 GitHub Pages에서 깨진다.

7. **파일 하나가 500줄을 넘으면 분리한다.**

8. **한국어 조사를 문자열로 이어 붙이지 않는다.** `src/core/ko.js` 를 쓴다.
   "채팅다", "프로젝트이다" 같은 문장이 화면에 나오면 그 자체로 결함이다.
   `(으)로` 처럼 양쪽을 다 적는 것도 안 된다 — 그 괄호가 화면에 그대로 나간다.
   앞말이 `<b>` 안에 있어 이어 붙일 수 없으면 조사만 받아 쓴다(`euroJosa`).
   **조사는 앞말에 붙여 쓴다**(맞춤법 41항). 영문 뒤에서도 같다 — `RAG 를` 이 아니라 `RAG를`.

9. **파일 수정은 Edit/Write 도구로만.** 이 PC의 PowerShell 5.1로 한글 파일을 통째로 읽어 다시 쓰면
   인코딩이 깨진다(실제 사고 이력 있음). PowerShell은 명령 실행에만 쓴다.

10. **브라우저 확인은 새 탭에서.** 사용자가 쓰고 있는 크롬 창을 건드리지 않는다.

---

## 2. 콘텐츠 규칙

- **특정 회사·조직명, 사내 시스템명을 넣지 않는다.** 공개 저장소에 올라간다. 상황 카드는 일반화된 업무로 쓴다.
- **가격·벤치마크 점수·순위 금지.** 몇 주면 틀린 정보가 된다. `무료 / 부분 무료 / 유료` 정도의 거친 구분만.
- **모델 버전 번호를 박지 않는다.** 계열명(Claude Opus, GPT-5 계열, Gemini)까지만.
- **한 회사 제품 이름으로 개념을 가르치지 않는다.** 갈래를 가르치고 제품은 예시로 든다.
  터미널 도구는 `CLI 에이전트`(claude · codex · gemini), 지침 파일은 `지침 파일`
  (CLAUDE.md / AGENTS.md / GEMINI.md). 사내 교육 자료가 한 회사 이름으로 굴러가면
  회사가 도구를 바꾸는 순간 자료 전체를 다시 만들어야 한다.
  → 갈래를 설명하는 용어는 `source` 에 **배열**을 넣어 회사별 공식 문서를 다 건다.
- **결과 화면에 같은 문장이 두 번 나오면 안 된다.** 같은 이름의 사유는 한 줄로
  묶여 "3건" 이 붙는다(`core/sim.js` 의 `groupReasons`). 그래서 `applyGain` 으로
  같은 잘한 점을 두 번 줄 때는 **"N건" 이 뜻이 통하는지** 확인한다.
  가드레일 두 개를 걸었으면 "가드레일 2건" 은 통한다. 한 가지 잘한 점을 두 조건이
  각자 세는 것은 안 통한다 — 그때는 이름을 나눠야 한다(1번 판의 `형식 지정`).
- **도구 소개에 단점 칸을 비워두지 않는다.** 장점만 나열하면 그 자체가 워크슬롭이다.
- 모든 콘텐츠 데이터에 **`checkedAt`** 을 넣는다.
- 개발자 전용 표현이 **게임 화면에** 나오면 안 된다 ("레포 클론", "npm run dev").
- **한 화면에 설명 3줄 초과 금지.**

---

## 3. 폴더 구조 — 이 배치가 계약이다

```
src/
├─ main.js         화면 전환 + 엔진 등록만. 게임 로직 금지
├─ core/           state(저장) · who(학습자 칸) · router · sim(판정로그) · ko(조사)
│                  motion · loop(실시간 루프) · sfx(효과음) · art(인라인 SVG)
├─ shell/          공통 셸 — 게임 내용을 몰라야 한다
├─ engines/        게임 템플릿 11종 — 특정 미니게임을 몰라야 한다
├─ games/          미니게임 = 엔진 지정 + 데이터. 파일 하나가 게임 하나
└─ data/           terms · products · tools
```

**엔진 11종** — 세팅형(A~E)과 액션형(F~H), 그리고 메커닉 엔진(W·P·L·T).

| | 이름 | 조작 | 쓰는 판 |
|---|---|---|---|
| A | 분류 | 통에 넣는다 | 5·17 |
| B | 판별 | 골라서 확인 | 4 |
| C | 조립 | 부품을 슬롯에 | 1·9·10·16 |
| E | 미리보기 | 켜고 끄며 결과 대조 | 3 |
| **W** | **배선** | 상자를 선으로 이어 흐름을 만든다 | 11·12 |
| **P** | **예측** | 진실을 보기 **전에** 먼저 건다 | 7 |
| **L** | **조립 라인** | 엔진을 차체에 끼워 출고한다 | 6 |
| **T** | **발동 타임라인** | 하루에 걸어 두고 **한 번 돌린다** | 8 |
| **F** | **디펜스** | 내려오는 것을 막는다 (실시간) | 13 |
| **G** | **슈팅** | 문제 있는 것만 쏜다 (실시간) | 2·15 |
| **H** | **검토 라인** | 창구에서 승인/반려 (실시간) | 14 |

액션형(F~H)이 공유하는 것: `core/loop.js`(시간) · `core/sfx.js`(소리) ·
`core/art.js`(그림) · `assets/arcade.css` 의 `ax__*` 부품.
엔진 전용 스타일은 `def__*` · `sh__*` · `qc__*` 로 따로 둔다.

메커닉 엔진은 **계산을 `core/` 로 올려 판정과 공유한다** — 화면에 뜬 것과
판정이 어긋나면 그 판은 아무것도 못 가르치기 때문이다.
W 는 `core/graph.js`(일정), T 는 `core/fire.js`(발동 시점)를 쓴다.
스타일은 `wire.css`(`wf__*`) · `mech.css`(`bt__*` · `asm__*`) ·
`timeline.css`(`tl__*`).

**엔진 D(배치형)는 없앴다.** 칸에 이름표를 떨어뜨리는 방식이라 조작이
서식 채우기와 구별되지 않았다. 11·12번은 W 로, 8번은 T 로 갔다.
**다시 만들지 말 것.**

**계층 규칙**: `engines/` 는 `shell/` 을 직접 조작하지 않는다.
엔진은 판정 결과(`ctx.finish(result)`)를 돌려주기만 하고, 그리는 것은 셸이 한다.
셸도 엔진을 import 하지 않는다 — 공유할 것이 있으면 `core/` 로 올린다.

**용어 수집**: **획득**(게임에서 딴 것)과 **읽음**(도감에서 펼쳐 본 것)은 다른 것이다.
화면에 찍는 숫자는 전부 **획득**이고, 획득 수는 `shell/collect.js` 에서만 센다 —
세 화면(상단바·코스맵·결과 카드)이 각자 세면 숫자가 어긋난다.
둘을 합치지 말 것. 합치면 도감을 열어 본 것까지 세어서 모으는 느낌이 사라진다.

**학습자 칸**: 저장 키는 **지금 학습자 한 명당 하나**다 (`core/who.js`).
**1번 사람은 접미사 없이 `ai-course-v1` 을 쓴다 — 이걸 바꾸면 이미 진행한
사람 전원의 기록이 날아간다.** 사람을 바꾸거나 이름을 고치면 `location.reload()`
한다. 다시 그리는 것만으로는 이미 그려진 화면에 앞사람 숫자가 남는다.
새 저장 키를 `who.js` 밖에서 만들지 않는다 — '처음부터'가 전부 지우지 못한다.

**미니게임 추가 = `games/` 파일 하나 + `games/index.js` 에서 `ready: true`.**
이 두 곳 말고 다른 파일을 고쳐야 한다면 구조가 잘못된 것이다.

---

## 4. 작업 사이클

```
1) PROGRESS.md 에서 "다음 할 일" 확인
2) 변경 → start.cmd 로 띄워 브라우저에서 눈으로 확인
3) 정답 경로 / 오답 경로 두 번 다 돌려 본다
4) PROGRESS.md 갱신
```

### 미니게임 하나를 끝냈다고 말하기 전에

- [ ] `node _tests/run-all.mjs` 전부 통과
- [ ] 정답 경로 완주 → 승인 도장
- [ ] 오답 경로 완주 → 반려 도장 + 왜 틀렸는지가 화면에 뜸
- [ ] 360px에서 가로 스크롤 없음
- [ ] 콘솔 에러 0
- [ ] 조작 대상이 전부 44px 이상
- [ ] 외부 요청 0건

### 판정 규칙은 브라우저 없이 검사한다

`simulate()` 는 DOM 을 안 쓰는 순수 함수다. 그래서 Node 로 바로 돌릴 수 있고,
등급·점수·실패 사유 이름·콘텐츠 짜임새는 전부 여기서 잡는다.

```
node _tests/run-all.mjs
```

미니게임을 추가·수정하면 `_tests/` 에 그 판의 검사도 같이 넣는다.
**"화면에서 해봤다"만으로는 오답 경로 네 가지를 다 밟을 수 없다.**

### 글을 고쳤으면 두 가지를 더 돌린다

- `test-content.mjs` — concept 에 적은 용어가 그 판 화면에 실제로 나오는지,
  용어처럼 굵게 쓴 말이 도감에 있는지, 조사 붙여쓰기가 맞는지를 본다.
- `test-font.mjs` — **한글을 새로 쓰면 글꼴 조각을 다시 만들어야 한다.**
  조각에 없는 음절은 그 글자만 시스템 글꼴로 나와서 한 단어 안에서 글꼴이 갈린다.
  빠진 글자를 찍어 주고, 다시 만드는 명령은 `assets/vendor/pretendard/README.md` 에 있다.

### 브라우저에서 확인할 때 — 모듈 캐시

`import()` 로 불러오는 파일은 하드 리로드로도 새로 안 받아진다. 고친 게 화면에
안 보이면 **버그가 아니라 캐시**인 경우가 많다. 다른 포트로 서버를 하나 더 띄우면
출처가 달라져서 전부 새로 받는다 (`python -m http.server 8766`).

### 3번 룰
**같은 증상이 3번 반복되면 멈춘다.** 다른 방식으로 만들거나 그 기능을 버린다.
사용자에게 상황을 보고하고 판단을 요청한다.

---

## 5. 세션 종료 시

`PROGRESS.md` 를 갱신한다. 다음 세션이 그 파일만 읽고 이어받을 수 있어야 한다.
- 지금 되는 것 / 안 되는 것
- 다음 할 일 (우선순위 순 3개)
- 알려진 함정
