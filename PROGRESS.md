# PROGRESS.md

> 마지막 갱신: **2026-08-17** — 미니게임 17개 전부 완성. 화면 5개 전부 동작.

---

## 현재 상태

| 항목 | 수 | 비고 |
|---|---|---|
| 미니게임 | **17 / 17** | `games/index.js` 의 `manifest` 17줄 전부 `ready: true` |
| 필수 / 심화 | 10 / 7 | 필수 1·2·3·5·6·7·8·15·16·17 |
| 엔진 | **5종** | A 분류(5·6·8·17) · B 판별(2·4·7·14·15) · C 조립(1·9·10·16) · D 배치(11·12·13) · E 미리보기(3) |
| 용어 도감 | **59개** | 챕터1 12 · 챕터2 15 · 챕터3 9 · 챕터4 12 · 더 알아두면 좋은 것 8 · 번외 3 |
| AI 도구 도감 | 카테고리 **11개** · 도구 **35개** | `data/tools.js` 의 `toolCount` 가 계산해서 화면에 찍는다 |
| 소스 | `src/` 아래 42개 파일 · 약 5,600줄 | 제일 긴 파일이 321줄 — 500줄 넘는 파일 없음 |

판정과 실행 로그는 전부 규칙이다. LLM 호출도 외부 요청도 없다 (`core/sim.js`).

---

## 화면별 상태

| 화면 | 주소 | 상태 |
|---|---|---|
| 코스맵 | `#/` | 챕터 6블록(1·2·3·4·5·번외)을 `manifest` 만 보고 그린다. 맨 위 "이어서 하기" 가 다음에 누를 것을 정해 준다. 진행도·랭크·"처음부터" 버튼 있음 |
| 게임 | `#/game/:id` | 엔진 5종이 무대만 그린다. 판정은 도장 + 해설 패널(정답/오답 사유 + 해금 용어 칩) |
| 용어 도감 | `#/codex` | 59개 · 챕터별 묶음 · 검색(한글·영문·비유·설명 전문 검색) · 출처 링크 · 확인 시점 표시. 게임을 안 해도 전부 열람 가능하고 수집한 것만 ✓ 로 표시된다 |
| AI 도구 도감 | `#/tools` | 상단 경고 배너 고정 · 애그리게이터 설명 · 카테고리 11개 · 검색. 도구 카드마다 주의점·용도·거친 가격 구분·공식 링크 |
| 결과 카드 | `#/report` | 깬 코스 / 필수 / 모은 용어 3칸 + 성향 한 줄. 숫자가 올라가는 연출. 개인정보 없음 — 캡처해서 공유 가능 |

저장은 localStorage 키 하나(`ai-course-v1`). 최고 점수만 남기고 낮은 점수로 덮어쓰지 않는다.
버전이 다르면 조용히 초기화한다(마이그레이션 코드 없음).

---

## 확인된 어긋남 (고치면 좋은 것, 지금도 게임은 돈다)

문서를 갱신하면서 코드를 대조해 찾은 것이다. 셋 다 콘텐츠 연결 문제이고 동작 오류는 아니다.

1. **미니게임 10의 해금 용어 이름이 도감과 한 글자 다르다.**
   게임은 `'CLAUDE.md·AGENTS.md'`, 도감은 `'CLAUDE.md · AGENTS.md'`(가운뎃점 양옆 공백).
   문자열이 안 맞아서 이 용어에는 ✓ 가 영원히 안 붙는다.
2. **미니게임 9의 `'최소 권한'` 은 도감에 없는 용어다.** 해금 목록에는 들어가는데 대응하는 카드가 없다.
3. **용어 59개 중 게임으로 모을 수 있는 것은 44개다.** 나머지 15개는 어떤 게임의 `concept` 에도 없다
   (멀티모달, 추론 모델, 파인튜닝 vs 프롬프팅, 툴 콜링, 컨텍스트 압축, 체크포인트, 바이브 코딩,
   스펙 주도 개발, 챕터 6의 8개, 로컬 실행).
   결과 카드는 "모은 용어 (전 59)" 로 찍으므로 분모에 절대 도달할 수 없다.
   → 읽기 전용 용어라면 결과 카드 분모를 해금 가능한 수로 바꾸는 편이 정직하다.

---

## 알려진 함정

- **`index.html` 더블클릭으로는 안 열린다.** ES 모듈 + `file://` 은 브라우저가 막는다.
  `start.cmd` 를 쓰거나 배포 주소로 연다. 이 상황을 안내하는 화면이 `index.html` 안에 들어 있다.
- **브라우저가 ES 모듈을 캐시한다.** 코드를 고쳤는데 화면이 그대로면 대개 이것이다.
  새로고침(F5)이 아니라 **하드 리로드**(Ctrl+Shift+R, 또는 개발자도구를 연 채 새로고침 버튼 길게 누르기)를 한다.
  고친 게 반영 안 된다고 코드를 다시 뜯지 말 것.
- **백그라운드 탭에서는 `requestAnimationFrame` 이 멈춘다.** 애니메이션이 시작 프레임에 얼어붙어
  화면이 통째로 비어 보이거나 점수가 0에 멈춘다. 실제로 겪은 증상이라
  `core/motion.js` 의 등장(`enter`·`cardIn`)·도장(`stamp`)·점수(`countUp`)에 `setTimeout` 안전망을 넣어 두었다.
  **모션을 새로 추가할 때도 같은 안전망을 반드시 같이 넣는다.** 없으면 그 요소만 안 보인다.
- **PowerShell로 한글 파일을 고치면 깨진다.** 이 PC의 PowerShell 5.1 인코딩 문제다(사고 이력 있음).
  파일 수정은 Edit/Write 도구로만 한다. PowerShell은 명령 실행에만 쓴다.
- **콘텐츠 강조는 `**이렇게**` 로 적는다.** `games/` 와 `data/` 의 문구에 HTML 태그를 직접 쓰지 않는다.
  `core/text.js` 의 `strong()` 이 이스케이프를 먼저 하고 `<b>` 로 바꾼다.
  화면에 별표가 그대로 보이면 그 문구를 `strong()` 안 거치고 넣은 것이다.
- **한국어 조사를 문자열로 이어 붙이면 "채팅다" 가 나온다.** `core/ko.js` 를 쓴다.
  영문 이름의 받침 판정이 필요한 경우는 `ko.js` 의 `EXCEPTIONS` 에 추가한다.
- 주소창에 같은 해시를 다시 입력해도 게임이 재시작되지 않는다. 앱 안에서 `go()` 로 이동할 때는
  라우터가 직접 처리하지만, 주소창으로 테스트할 때는 하드 리로드하거나 `?r=2` 같은 쿼리를 붙인다.
- 브라우저 스크린샷 도구가 간헐적으로 타임아웃한다. 페이지 문제가 아니다. 2~3초 뒤 다시 찍으면 된다.

개발 확인용 자동 플레이 하네스가 루트에 `_test.js` 로 있다(`.gitignore` 로 배포본에서 빠진다).
각 게임의 `simulate()` 로 이기는 세팅·지는 세팅을 먼저 찾은 뒤 실제 버튼을 눌러 끝까지 돌린다.

---

## 다음에 할 수 있는 일 (우선순위)

1. **저장소로 만들고 실제 배포 주소에서 한 바퀴 돌린다.** 아직 `git init` 을 안 했다.
   push → Settings → Pages(`main` / `/ (root)`) → 열린 주소에서 코스맵·게임·도감 2종·결과 카드를 확인한다.
   `.nojekyll` 이 올라갔는지, 상대경로가 저장소 이름 아래에서도 깨지지 않는지가 확인 대상이다.
   휴대폰으로도 한 번 연다. 사내 배포 전에 반드시 거쳐야 하는 단계다.
2. **위 "확인된 어긋남" 3건을 정리한다.** 1·2번은 문자열 한 줄씩이고, 3번은 결과 카드 분모를
   해금 가능한 용어 수로 바꾸거나 남은 15개에 해금 경로를 주는 판단이 필요하다.
   콘텐츠 문구를 고칠 때는 `TERMS.md` → `data/*.js` 방향을 지킨다.
3. **17판 전수 통과 기록을 남긴다.** `_test.js` 로 정답 경로·오답 경로를 두 번씩 돌려
   도장 표시·해설 3줄·콘솔 에러 0·외부 요청 0건을 게임별로 확인하고 결과를 이 파일에 표로 붙인다.
   지금까지의 확인은 게임을 만들 때마다 각각 한 것이고, 17개를 한 번에 통과시킨 기록은 없다.

---

## 확정된 설계 결정 (다시 논의하지 말 것)

| 결정 | 내용 |
|---|---|
| 코스 구성 | **필수 10 + 심화 7.** 17개 전부 만들되 필수만으로 완주(약 17분) |
| 필수 10개 | 1·2·3 · 5·6·7·8 · 15 · 16 · 17 |
| 심화 7개 | 4 · 9 · 10·11·12 · 13·14 (챕터 3은 통째로 심화) |
| 조작 | **탭-투-탭이 기본**, 드래그는 데스크톱 보너스 |
| 종합 게임(16) | 필수만으로도 완결. 심화를 깨면 부품이 추가 해금됨 |
| 검증 범위 | 챕터 2 전수 검증 + 도구 도감은 선별 검증 |
| 시그니처 | 작업 지시서(티켓) + 결재 도장. 애니메이션은 도장과 "작업 중"에만 |
| 판정 | 전부 규칙. LLM 호출 없음. `core/sim.js` 가 실행 로그까지 만든다 |
| 도구 도감 | 카테고리가 뼈대, 도구는 예시. 순위·가격·점수 없음 |
