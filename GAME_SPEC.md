# GAME_SPEC.md — 설계 명세 (1단계 산출물)

> AI 개념 학습 웹게임. `AI-GAME-PROMPT.md`의 요구사항을 실행 가능한 설계로 옮긴 문서다.
> **이 문서에 없는 구조는 만들지 않는다.** 콘텐츠(문항 텍스트·용어 정의·도구 정보)는
> `TERMS.md` / `data/*.js`에 있다. 여기엔 **틀과 판정 규칙만** 있다.
>
> 구현이 끝난 뒤 **실제 코드에 맞춰 정정했다.** 이 문서는 이제 만들려던 것이 아니라
> **만들어진 것**을 설명한다. 문서와 코드가 어긋나면 코드가 맞다.

| 항목 | 값 |
|---|---|
| 작성 | 2026-08-17 (설계) |
| 정정 | 2026-08-17 — 구현 완료본과 대조 |
| 단계 | 3단계 완료 · 미니게임 17개 전부 동작 |
| 확정 결정 | 필수 10 / 심화 7 · 챕터2 전수검증 + 도감 선별검증 · 3단계까지 진행 |

---

## 1. 확정된 설계 결정

프롬프트에 명시되지 않았거나 서로 충돌해서 이번에 결정한 항목이다.

### 1-1. 플레이 시간 — 필수 10 + 심화 7

프롬프트 §3은 "노드 하나 2분 이내", §12는 "전체 20분 이내"인데 17개면 34분이라 모순이다.
**필수 코스 10개(약 17분)** 를 완주 기준으로 삼고, 나머지 7개는 코스맵에서 선택 플레이한다.
17개를 전부 만드는 것은 변함없다.

| 게임당 예산 | 초 |
|---|---|
| 조작 | 60 |
| 판정 + 해설 패널 | 30 |
| 노드 이동·전환 | 10 |
| **합계** | **100초** → 필수 10개 = 16분 40초 |

### 1-2. 종합 게임의 부품은 심화 클리어로 늘어난다

미니게임 16(업무 티켓 처리)은 **필수 코스만 깬 사람도 완결**되어야 한다.
심화를 깨면 서브에이전트·훅 같은 부품이 추가 해금되어 더 나은 해법이 가능해진다.
→ 심화가 "안 해도 되는 것"이 아니라 "하면 더 잘 푸는 것"이 된다.

### 1-3. 조작은 탭-투-탭이 기본, 드래그는 데스크톱 보너스

프롬프트 §2가 허용한 대안을 **처음부터 기본**으로 잡는다.
모바일 HTML5 드래그앤드롭은 브라우저·기기별로 실패가 잦고, 실패하면 게임 전체가 죽는다.

```
집는다(탭) → 대상이 하이라이트 → 놓는다(탭)
데스크톱: 위와 동일 + 포인터 드래그도 허용 (같은 함수 호출)
키보드:   Tab 이동 → Enter로 집기 → Tab 이동 → Enter로 놓기
```

### 1-4. 챕터3(일 시키기)은 통째로 심화

필수 코스는 **이해 → 구분 → 위험 → 종합**으로 짧게 끊고,
"AI에게 일을 어떻게 시키는가"(위임·자동화 설계)는 관심 있는 사람만 들어간다.

---

## 2. 코스 구성

```
챕터1 AI 기본기      ●─●─●─○
챕터2 도구 구분 ★    ●─●─●─●─○
챕터3 일 시키기      ○─○─○          (전체 심화)
챕터4 안전장치       ○─○─●
챕터5 실전 종합      ●
번외  이 일엔 어떤 AI ●  →  [AI 도구 도감] 상시 열람

● 필수 10개   ○ 심화 7개
```

| # | 미니게임 | 엔진 | 필수 | 통과 시 수집 용어 | 조작이 이 개념에 맞는 이유 |
|---|---|---|---|---|---|
| 1 | 가방 싸기 | C 조립형 | ● | 토큰, 컨텍스트 윈도우, 컨텍스트 로트 | 한계가 **칸 수**로 눈에 보이고, 넘칠 때 먼저 것이 밀려나가는 걸 직접 목격해야 이해된다 |
| 2 | 지어낸 답 찾기 | B 판별형 | ● | 할루시네이션, 시스템 프롬프트 | 지어낸 문장을 **직접 찾아내야** 남의 말로 듣는 것과 달라진다 |
| 3 | 지시문 고쳐쓰기 | E 미리보기 | ● | 프롬프트 엔지니어링, 시스템 프롬프트 | 태그 하나 붙일 때마다 **결과가 실시간으로 바뀌는** 인과를 봐야 한다 |
| 4 | 자료 찾아오기 | B 판별형 | ○ | RAG, 임베딩·벡터DB | 엉뚱한 자료 → 엉뚱한 답이라는 인과를 자기 손으로 만들어봐야 한다 |
| 5 | 어디서 할까? | A 분류형 | ● | 프로젝트, 아티팩트, Claude Code, Cowork | 다섯 개가 **동시에 보이는 통**으로 놓여야 차이가 대비된다 |
| 6 | 모델이냐 제품이냐 | A 분류형 | ● | 모델, 제품, 표면(surface) | 이름을 빠르게 양쪽으로 던지는 반복이 분류 기준을 몸에 남긴다 |
| 7 | 이름에 속지 마라 | B 판별형 | ● | Claude Code, Codex | 오해를 **하나씩 깨는** 형식이라야 "나도 저렇게 알고 있었네"가 나온다 |
| 8 | 도구함 정렬 | A 분류형 | ● | 커맨드, 스킬, 서브에이전트, 훅, 플러그인, MCP | 제일 헷갈리는 6형제는 **반복 분류**만이 답이다 |
| 9 | 배선 연결 | C 조립형 | ○ | MCP, 커넥터, 최소 권한 | 연결과 권한은 **선을 잇는** 물리적 은유가 가장 빠르다 |
| 10 | 신입 온보딩 문서 | C 조립형 | ○ | CLAUDE.md, AGENTS.md, 컨텍스트 엔지니어링 | 많이 넣으면 오히려 나빠진다는 걸 **넣어보고 실패해야** 안다 |
| 11 | 팀 배치 | D 배치형 | ○ | 서브에이전트 vs 에이전트 팀, 오케스트레이션, 컨텍스트 격리 | 순차/병렬은 **타임라인 위 위치**로만 직관된다 |
| 12 | 자동화 라인 | D 배치형 | ○ | 에이전트 루프, 휴먼 인 더 루프, 휴먼 온 더 루프, 검증 루프 | 검토 지점을 **어디에 꽂을지** 스스로 정해봐야 한다 |
| 13 | 사고 막기 | D 배치형 | ○ | 훅, 하네스 엔지니어링, 가드레일, 샌드박스 | 시간 압박 속에서 배치해야 "늦으면 사고"가 체감된다 |
| 14 | 결재 반려 | B 판별형 | ○ | 워크슬롭, AI 슬롭, 검증 부채, AI 번아웃 · AI 피로 | 그럴듯한 걸 **시간 압박 속에 걸러내는** 경험이 곧 워크슬롭의 정의다 |
| 15 | 이상 징후 찾기 | B 판별형 | ● | 프롬프트 인젝션, 섀도우 AI, 자동화 편향, 감사 로그 | 위험은 **화면 속에 숨어 있어야** 찾는 눈이 생긴다 |
| 16 | 업무 티켓 처리 | C 조립형 | ● | 컨텍스트 엔지니어링, 가드레일, 휴먼 인 더 루프 | 배운 부품을 자유 조합해야 종합이 된다 |
| 17 | 이 일엔 어떤 AI? | A 분류형 | ● | 애그리게이터, 카테고리로 기억하기 | 도구 이름이 아니라 **카테고리**를 고르게 해야 목록이 안 낡는다 |

미니게임 16은 새 용어를 가르치지 않는 종합판이지만, 필수만 돈 사람에게는 이 셋을 처음 손에 넣는 자리가 된다.

### 엔진 A가 4번 반복되는 문제

필수 코스에 A가 4개(5·6·8·17)다. 같은 조작이 4번이면 프롬프트 §11의 "전부 퀴즈" 함정에 빠진다.
**A 엔진은 3가지 모드를 갖는다.** 겉보기·리듬·판정이 달라진다.

| 게임 | 모드 | 통 | 리듬 | 오답 처리 |
|---|---|---|---|---|
| 5 어디서 할까 | `study` | 5 | 시간제한 없음, 한 장씩 | 즉시 정답 위치 표시 + 이유 |
| 17 이 일엔 어떤 AI | `study` | 10 | 시간제한 없음, 한 장씩 | 즉시 정답 카테고리 표시 + 이유 |
| 6 모델이냐 제품이냐 | `rapid` | 2 | 연속 정답 콤보, 빠른 리듬 | 콤보 끊김 (최고 연속을 결과에 남김) |
| 8 도구함 정렬 | `drill` | 6 | 오답 카드가 뒤에 다시 나옴 | 한 줄 이유 후 재도전 (두 번째도 틀리면 넘어감) |

**`explore` 모드는 만들지 않았다.** 17번은 `study` 로 충분했고, 카테고리 설명은 이미
AI 도구 도감(`#/tools`)이 하고 있다. 모드 하나 때문에 엔진에 분기를 더할 값어치가 없었다.

---

## 3. 디자인 토큰

### 시그니처 하나: **작업 지시서(티켓)와 결재 도장**

소재는 "연수원 코스 + 작업 지시 + 실행 로그"다.
한국 사무직이 0초 만에 읽는 시각 언어는 **결재판**이다 — 종이 티켓이 올라오고, 판정이 **도장으로 찍힌다.**
미니게임 14가 실제로 "결재 반려"이므로 시그니처가 콘텐츠와 맞물린다.

- 도장은 **판정 순간에만** 등장한다. 장식으로 쓰지 않는다.
- 도장은 살짝 기울어 찍힌다. **주역은 도장과 "AI 작업 중" 로그 두 곳뿐이다.**
  나머지(등장·파문·날아가기)는 조작에 반응이 있다는 정도의 손맛까지만 쓴다 — 프리셋 목록은 아래 §모션 프리셋.

**자체 검토 — 아무 프로젝트에나 쓸 법한 기본값인가?**
아니다. 금지된 기본값 3종(크림+세리프+테라코타 / 검정+형광그린 / 그라데이션 남발)을 전부 피했고,
종이·도장은 이 게임의 주제(업무 판정·검증)에서 직접 나온 것이라 다른 프로젝트에 옮기면 어색해진다.

### 색 — 8개, 역할 고정

| 토큰 | 값 | 역할 | 흰 배경 대비 |
|---|---|---|---|
| `--paper` | `#E8ECF0` | 배경. 사무용지의 옅은 회청색. 크림색 아님 | — |
| `--card` | `#FFFFFF` | 티켓·카드 면 | — |
| `--on-mark` | `#FFFFFF` | 강조색 위에 얹는 글자 | — |
| `--ink` | `#16202B` | 본문 글자·테두리. 순검정 아님 | 15.9:1 |
| `--muted` | `#5F6B7A` | 비활성·보조 설명 | 5.4:1 |
| `--pass` | `#1D6E5A` | **성공** — 승인 도장 (녹청) | 6.1:1 |
| `--partial` | `#8A5E0A` | **부분 성공** — 조건부 승인 (황토) | 5.7:1 |
| `--fail` | `#A8322D` | **실패** — 반려 도장 (적) | 6.7:1 |
| `--mark` | `#2C4C7C` | 현재 위치·진행 표시 (남색 잉크) | 8.5:1 |

`--muted` 와 `--partial` 은 설계 당시 값(`#77828F` · `#B07A12`)이 4.5:1을 못 넘겨 구현하며 어둡게 고쳤다.
투명도 파생값(`--ink-06` … `--mark-16` 등)은 위 색에서만 만든다. **새 색을 추가하지 않는다.**

색만으로 상태를 구분하지 않는다. 도장 **문구**(승인/조건부/반려)와 **아이콘 형태**를 항상 함께 쓴다.

### 타이포 — 시스템 폰트만 (CDN 금지)

```css
--font-body: system-ui, -apple-system, "Segoe UI", "Malgun Gothic",
             "Apple SD Gothic Neo", sans-serif;
--font-mono: ui-monospace, "Cascadia Mono", Consolas, "D2Coding",
             "Nanum Gothic Coding", monospace;
```

크기는 **4단계 + 보조 2개**로 고정한다. 중간 크기를 즉흥으로 만들지 않는다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--fs-hero` | 22px | 화면 제목 |
| `--fs-title` | 18px | 구역 제목 |
| `--fs-body` | 16px | 티켓 본문 |
| `--fs-label` | 15px | 카드 라벨 |
| `--fs-mono` | 13.5px | 로그·수치 |
| `--fs-aux` | 13px | 보조 설명, `--muted` |

본문 16px 미만 금지 (모바일 확대 방지).

### 간격·크기

```css
--sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px;
--sp-4: 16px; --sp-5: 24px; --sp-6: 32px; --sp-7: 48px;
--radius: 6px;          /* 종이 느낌 — 둥글게 하지 않는다 */
--radius-sm: 3px;       /* 칩·포커스 링 */
--tap-min: 44px;        /* 모든 조작 대상 최소 크기 */
--shell-top: 56px;
--content-max: 720px;
```

그림자도 토큰이다(`--lift-1` · `--lift-2` · `--lift-3`) — 종이가 살짝 떠 있는 정도까지만.

### 모션

```css
--dur-tap:   160ms;  /* 카드 집기·놓기 */
--dur-move:  260ms;  /* 자리를 옮기는 것 */
--dur-stamp: 380ms;  /* 도장 찍힘 */
--ease:     cubic-bezier(.2, .7, .3, 1);
--ease-out: cubic-bezier(.16, 1, .3, 1);

@media (prefers-reduced-motion: reduce) { /* 전부 0ms, 도장은 즉시 표시 */ }
```

### 모션 프리셋 — `core/motion.js`

**anime.js 를 import 하는 파일은 이 하나뿐이다.** 라이브러리는 `assets/vendor/anime.esm.min.js` 에
넣어 두고 상대경로로 부른다 — CDN 을 쓰지 않는다(사내망 차단 대비). 다른 파일은 아래 프리셋만 부른다.
새 움직임이 필요하면 여기에 프리셋을 하나 더 만든다.

| 묶음 | 프리셋 |
|---|---|
| 등장 | `enter` · `cardIn` · `cardOut` · `stageIn` · `wipeIn` |
| 판정 | `stamp` · `shake` · `press` · `countUp` · `fillBar` · `burst` |
| 실행 로그 | `runLog` · `spin` · `typeIn` |
| 손맛 | `ripple` · `flyTo` · `pulse` |
| 도우미 | `isReduced` · `wait` |

**프리셋 전부가 지키는 규칙 하나 — `setTimeout` 안전망.**
백그라운드 탭에서는 브라우저가 `requestAnimationFrame` 을 멈춘다. 그러면 애니메이션이
**시작 프레임(투명)에 얼어붙은 채로 남아** 화면이 통째로 비어 보이거나 점수가 0에 멈춘다.
실제로 겪은 증상이라, 예상 시간이 지나면 안전망이 **최종 상태를 강제로 찍는다.**
`prefers-reduced-motion` 이면 애초에 최종 상태만 적용하고 끝낸다.
안전망 없는 프리셋을 추가하면 그 요소만 안 보이는 버그가 된다.

---

## 4. 공통 셸

**미니게임 17개가 제각각으로 보이면 안 된다.** 셸을 먼저 고정하고 각 게임은 무대 안에서만 달라진다.

```
┌──────────────────────────────────┐
│ ← 코스   ▓▓▓▓░░ 7/10   [도감]    │  상단바 56px 고정
├──────────────────────────────────┤
│                                  │
│          게임 무대                │  ← 엔진이 그리는 유일한 영역
│      (엔진 5종이 여기만 채움)      │
│                                  │
├──────────────────────────────────┤
│ 해설 패널 (판정 후 아래에서 올라옴) │  최대 3줄 + [다음]
└──────────────────────────────────┘
```

| 파일 | 책임 |
|---|---|
| `shell/topbar.js` | 뒤로가기, 랭크, 진행도 막대, 모은 용어 수, 도감 버튼(도감 안에서는 "닫기") |
| `shell/coursemap.js` | 노드 맵, 필수/심화 표시, 통과 ✓ / 반려 ↻ 표시, "이어서 하기", "처음부터" |
| `shell/debrief.js` | 판정 도장 + "방금 한 것의 이름" + 실패 사유·잘한 점 + 해설 3줄 + 수집된 용어 칩 |
| `shell/codex.js` | 용어 도감 (게임 밖에서도 열람) |
| `shell/toolbook.js` | AI 도구 도감 + 상단 고정 배너 |
| `shell/report.js` | 결과 카드 — 깬 코스·필수·모은 용어 3칸 + 성향 한 줄 |

**잠기는 노드는 없다.** 심화도 처음부터 들어갈 수 있다. 필수/심화는 "완주 기준"이지 자물쇠가 아니다.

**규칙: `engines/`는 셸을 직접 조작하지 않는다.** 엔진은 판정 결과를 반환만 하고, 셸이 그린다.
(`games_zombie/CLAUDE.md:119`의 단방향 계층 규칙을 그대로 가져온 것)

### 주소 — 해시 라우터 5개 (`core/router.js`)

| 주소 | 화면 |
|---|---|
| `#/` | 코스맵 |
| `#/game/:id` | 미니게임 |
| `#/codex` | 용어 도감 — `#/codex?q=아티팩트` 로 검색어를 넘긴다 |
| `#/tools` | AI 도구 도감 (`?q=` 동일하게 받는다) |
| `#/report` | 결과 카드 |

없는 주소는 코스맵으로 떨어진다. 게임 파일 하나가 깨져도 그 화면에서만 안내가 뜨고 앱은 계속 돈다.

### 랭크

| 랭크 | 조건 |
|---|---|
| 관람객 | 시작 |
| 프롬프터 | 챕터1 필수 완료 |
| 컨텍스트 설계자 | 챕터2 필수 완료 |
| 하네스 엔지니어 | 필수 10개 전부 + 심화 3개 이상 |

**여기서 "완료"는 전부 통과(`state.isCleared`)를 뜻한다.** 반려는 세지 않는다(§8).
랭크는 저장하지 않고 매번 계산한다. 올라가는 순간에만 화면에 한 번 알린다.

---

## 5. 엔진 5종 인터페이스

### 5-1. 모든 미니게임이 따르는 형태

```js
// games/05-where-to-work.js
export default {
  id: 'where-to-work',
  engine: 'A',
  mode: 'study',              // 엔진별 변주 모드 (없으면 그 엔진의 기본 모드)
  title: '어디서 할까?',
  subtitle: '한 줄 부제 — 제목 아래에 같이 뜬다',
  chapter: 2,
  required: true,             // ● 필수 / ○ 심화
  concept: ['프로젝트', '아티팩트', 'Claude Code', 'Cowork'],  // 통과 시 수집되는 용어
  timeLimit: null,            // null = 제한 없음 (엔진 B 만 읽는다)
  checkedAt: '2026-08',       // 콘텐츠 검증 시점
  data: { /* 엔진이 해석 */ },
  named: {                    // "방금 한 것의 이름" — 해설 패널 맨 위
    pass: '방금 한 것이 **컨텍스트 엔지니어링**이다', partial: '…', fail: '…'
  },
  debrief: {
    pass:    '3줄 이내',
    partial: '3줄 이내',
    fail:    '왜 틀렸는지 3줄 이내'
  },
  simulate(setup, data) { /* 엔진 C·D·E 와 일부 B 가 부른다. §6 */ }
}
```

**미니게임의 `data` 와 `simulate()` 는 그 게임 파일 안에서 끝난다.** `games/index.js` 의
`manifest` 에 한 줄(`ready: true`)을 더하면 코스맵에 뜬다. 그 둘 말고 고쳐야 할 파일이 있으면 구조가 잘못된 것이다.
게임 데이터는 진입할 때 동적 `import` 로만 불러온다 — 첫 화면에서 17개를 다 받지 않는다.

### 5-2. 엔진 공통 계약

```js
// engines/*.js — 5종 모두 이 2개만 노출한다
export function mount(root, game, ctx)   // root DOM에 무대를 그린다
export function unmount()                // 이벤트·타이머 전부 해제
// 종료는 ctx.finish(result) 호출로만
```

엔진 5종이 공유하는 부품은 `engines/base.js` 에 있다 — `el` · `header` · `brief` · `actions` ·
`runner`(실행 로그 패널) · `say`(스크린리더 알림) · `Bin`. `Bin` 이 붙인 이벤트를 모아 두었다가
`unmount()` 에서 한 번에 뗀다. 화면을 나갈 때 타이머·리스너가 남지 않는 이유가 이것이다.

```js
// 엔진이 셸에 돌려주는 유일한 형태
result = {
  grade: 'pass' | 'partial' | 'fail',
  score: 0~100,
  elapsed: 초,
  mistakes: [ { itemId, chose, correct, hint } ],  // 분류·판별형이 채운다
  faults:   [ { name, why } ],                     // 실행형이 채운다 — 실패 사유 (§6)
  gains:    [ { name, why } ],                     // 잘한 점
  stats:    [ { label, value } ],                  // rapid 모드의 "최고 연속" 같은 것
  unlocked: ['토큰', '컨텍스트 윈도우']              // 실제 수집된 용어
}
```

**반려면 `unlocked` 는 빈 배열이다.** 엔진 5종이 전부 같은 한 줄을 쓴다 —
`unlocked: grade === 'fail' ? [] : (game.concept || [])`. 통과해야 용어가 붙는다.

`grade`가 3단계인 이유: 프롬프트 §5의 미니게임 16이 **성공/부분성공/실패** 판정을 요구한다.
나머지 게임도 같은 척도를 쓰면 해설 패널과 결과 카드가 하나로 통일된다.

### 5-3. 엔진별 data 스키마

#### A. 분류형 (`engines/sort.js`) — 게임 5·6·8·17

```js
data: {
  bins:  [ { id, label, hint } ],        // 통 (2~10개)
  cards: [ { id, text, bin, why } ],     // why = 판정 뒤 보여줄 한 줄
  pass: { minCorrect: 10 }               // 통과 기준 (없으면 전체의 80%)
}
```
모드별 차이는 §2 표의 3모드. 통이 5개를 넘으면 통을 가로 스크롤 대신 **2행 그리드**로 접는다(360px 대응).

#### B. 판별형 (`engines/spot.js`) — 게임 2·4·7·14·15

```js
// pick 모드 (기본) — 게임 2·4·14·15
data: {
  scene:   { cap, body },                // 대조할 근거 자료 (선택)
  prompt:  '위험한 것 4건을 고르시오',     // 무대 위 한 줄 지시 (선택)
  layout:  'board',                      // 시각이 붙은 줄 목록 — 게임 15
  targets: [ { id, label, tag, danger: true|false, why } ],
  need:    4                             // 찾아야 할 개수
}
// myth 모드 — 게임 7. 주장 한 장씩 내고 사실/오해로 답한다
data: { items: [ { id, claim, isTrue, truth } ] }
```
- `layout: 'board'` 는 미니게임 15(사무실 하루) 용이다. 좌표 배치가 아니라 **시각이 붙은 줄 목록**으로 만들었다 —
  좌표는 360px에서 무너지고, 실제로 훑어보는 방식은 시간 순이다.
- **오클릭 3회 실패(`penalty: 'strike'`)는 만들지 않았다.** 점수식이 대신한다:
  `맞힌 수 / need × 100 − 헛짚은 수 × 15`, 80점 이상 승인 · 50점 이상 조건부.
  한 번 잘못 짚었다고 판이 끝나면 "왜 위험한지"를 읽을 기회가 사라진다.
- 시간 제한은 `data` 가 아니라 `game.timeLimit`(초)에 둔다 — 게임 14가 75초. 0이 되면 그 자리에서 판정한다.
- `game.simulate(고른것, data)` 가 있으면 판정 뒤 실행 로그가 흐르고 그 결과가 등급을 덮어쓴다.

#### C. 조립형 (`engines/build.js`) — 게임 1·9·10·16

배치 방식이 두 가지다. `slots` 가 있으면 슬롯형, 없으면 예산형으로 그린다.

```js
// 예산형 — 게임 1. 칸이 정해진 통 하나
data: {
  budget: 6, budgetLabel: 'AI의 책상',
  parts: [ { id, label, cost, danger, note } ]
}
// 슬롯형 — 게임 9·10·16. 역할이 정해진 여러 칸
data: {
  slots: [ { id, label, accepts: ['source'], capacity } ],
  parts: [ { id, label, tags: ['source'], danger, note } ]
}
// 공통(선택): brief · briefCap · trayLabel · runLabel · runCaption
```
**게임 1의 핵심 연출**: `budget` 초과 시 **먼저 넣은** 부품이 회색으로 밀려나간다. 이게 컨텍스트 로트다.
밀려나는 규칙은 엔진에 박아 두었다(뒤에서부터 채운다) — `overflow: 'evict-oldest'` 같은 설정값은 두지 않았다.
설정으로 뺄 만큼 종류가 생기지 않았고, 값 하나로 연출이 바뀌는 척하는 게 더 위험하다.

판정은 `game.simulate(setup, data)` 가 한다. 엔진은 세팅만 모아서 넘긴다:
`setup = { kept, evicted, slots, all, used, cap }`. 룰셋 이름(`evaluate`)은 쓰지 않는다.

#### D. 배치형 (`engines/timeline.js`) — 게임 11·12·13

```js
data: {
  lanes: 2,                              // 병렬 레인 수 (게임 13은 1)
  ticks: 8,                              // 타임라인 칸
  laneNames: ['에이전트'],
  jobs:  [ { id, label, dur } ],
  gates: [ { id, label, kind: 'review'|'hook'|'sandbox'|'verify' } ],
  hazards: [ { atTick, label } ]         // 게임 13의 사고 지점 — 빨간 칸으로 뜬다
}
```
엔진이 하는 일은 **겹치지 않게 놓는 것까지**다. 판정은 `game.simulate({ plan, lanes, ticks }, data)`.
`blockedBy`(무엇으로 막히는가)는 **게임 파일 안에만** 있다 — 엔진이 사고 규칙을 알면 안 되기 때문이다.
순서대로만 놓으면 마감을 넘기고(`마감 초과`), 병렬로 몰면 합류에서 깨진다(`합류 실패`).

#### E. 미리보기형 (`engines/preview.js`) — 게임 3

```js
data: {
  toggles: [ { id: 'role', label: '역할', tag: '누가', desc: '누구의 입장에서 볼 것인가' } ],
  outCap: 'AI가 내놓는 결과', runLabel: '이대로 보내기', runCaption: '완성된 지시로 일을 시킨다'
}
```
판정 규칙은 `data` 가 아니라 게임 파일의 함수 두 개에 있다.

- `game.render(켜진목록, data)` → `{ prompt, output, say }`. 토글을 켤 때마다 **오른쪽 결과가 그 자리에서 다시 쓰인다.**
- `game.simulate(켜진목록, data)` → [보내기] 를 눌렀을 때의 판정.

**LLM 호출 없음.** 토글 조합마다 미리 써 둔 출력 조각을 규칙대로 이어 붙인다.

---

## 6. 판정은 전부 룰 엔진

API 호출이 금지되어 있으므로 모든 판정은 순수 함수다.
**`core/rules.js` 는 만들지 않았다.** 판정이 게임마다 너무 달라서 하나의 규칙 표로 묶이지 않았다.
억지로 묶었으면 "규칙 표"가 사실상 17개의 분기가 됐을 것이다. 대신 두 층으로 나눴다.

| 층 | 파일 | 하는 일 |
|---|---|---|
| 공용 | `core/sim.js` | 실행 로그 빌더 `Run` + 공통 결함(FAULT)·이득(GAIN) 표 |
| 게임별 | `games/NN-*.js` 의 `simulate()` | 그 게임만의 판정 규칙 |

```js
// core/sim.js — 체이닝으로 로그를 쌓고 마지막에 등급을 낸다
const r = new Run();
r.read('규정집을 읽는다').do('초안을 만든다').warn('근거가 없다').out('보고서 초안 완성');
applyFault(r, 'halluc', 22);   // FAULT 표에서 이름·설명을 꺼내 점수를 깎는다
applyGain(r, 'gate', 10);      // GAIN 표 — 잘한 점은 점수를 올린다
return r.finish({ pass: 85, partial: 55 });
// → { grade, score, steps, faults, gains }
```

`FAULT` / `GAIN` 은 **이름표를 한 곳에 모아 둔 것**이다. 챕터가 달라도 같은 사고는 같은 용어로 배운다.

| | 이름 |
|---|---|
| `FAULT` | 컨텍스트 초과 · 컨텍스트 로트 · 환각 · 워크슬롭 · 정보 유출 · 프롬프트 인젝션 · 합류 실패 · 마감 초과 · 검토 없음 |
| `GAIN` | 근거 확인 · 컨텍스트 엔지니어링 · 병렬 처리 · 휴먼 인 더 루프 · 가드레일 · 최소 권한 |

`Run` 이 쌓은 `steps` 는 그대로 **화면에 한 줄씩 흐른다**(`base.js` 의 `runner`).
점수만 통보하지 않고 과정을 보여주는 부분이 여기다. 줄 종류(`read`/`do`/`ok`/`warn`/`fail`/`out`)마다
색과 머무는 시간이 다르다 — 위험 신호와 사고는 더 오래 멈춘다.

미니게임 16(종합)만 규칙이 복합적이다. 티켓 1건에 대해 **자료 · 방식 · 안전장치** 세 칸을 순서대로 검사한다.

| 검사 | 판정 |
|---|---|
| 자료 칸이 비었다 / 규정집이 없다 | 실패 — 환각 |
| 출처 미확인 자료를 넣었는데 사람 확인도 훅도 없다 | 실패 — 프롬프트 인젝션 |
| 안전장치가 없거나 "검토 없이 바로 보낸다" | 실패 — 검토 없음 |
| 자료를 과하게 넣어 규정 조항을 지나쳤다 | 감점 — 컨텍스트 로트 |
| 근거 표시 부품이 없다 | 부분 성공 — 워크슬롭 |
| 전부 통과 | 성공 |

실패 사유는 **결과 화면에 그 이름 그대로** 뜬다. 프롬프트 §0의 "실패했을 때 왜 실패했는지 즉시 이해" 조건.

**심화를 깬 사람에게는 부품이 늘어난다**(서브에이전트·샌드박스·훅 — §1-2). 화면에 들어갈 때마다
`state.isCleared()` 로 다시 계산하므로, 심화를 깨고 바로 여기로 와도 늘어난 부품이 보인다.

---

## 7. 용어 수집 매핑

**도감은 처음부터 전부 열람 가능**하다(프롬프트 §3). 수집은 "모았다"는 표시일 뿐 열람을 막지 않는다.
그리고 수집 경로가 **둘**이다.

1. **게임을 통과하면** 그 게임의 `concept` 목록이 수집된다. 반려면 수집되지 않는다.
2. **도감에서 카드를 펼쳐 읽으면** 그 용어가 수집된다(`state.unlockTerm`).
   게임으로만 모으게 하면 어떤 게임에도 안 묶인 용어는 영영 미수집으로 남는다.
   도감을 실제로 읽게 만드는 것도 이 교육의 목적이라 이 경로를 넣었다.

| 챕터 | 용어 수 | 주요 용어 |
|---|---|---|
| 1 AI 기본기 | 12 | 토큰, 컨텍스트 윈도우, 컨텍스트 로트, 할루시네이션, 시스템 프롬프트, 프롬프트 엔지니어링, RAG, 임베딩·벡터DB |
| 2 도구 구분 | 16 | 모델/제품/표면, 프로젝트, 아티팩트, Claude Code, Cowork, Codex, 커맨드, 스킬, 서브에이전트, 훅, 플러그인, MCP, 커넥터, 최소 권한 |
| 3 일 시키기 | 9 | CLAUDE.md · AGENTS.md, 컨텍스트 엔지니어링, 에이전트 루프, 오케스트레이션, 휴먼 인 더 루프 |
| 4 안전장치 | 12 | 하네스 엔지니어링, 가드레일, 샌드박스, 워크슬롭, AI 슬롭, 검증 부채, 프롬프트 인젝션, 섀도우 AI, 자동화 편향 |
| 6 더 알아두면 좋은 것 | 8 | 에이전틱 AI 등 — 미니게임에 묶이지 않은, 도감으로만 읽는 칸 |
| 번외 | 3 | 애그리게이터, 로컬 실행, 멀티모달 |

**합계 60개** — 프롬프트 §12의 "35개 이상"을 넘겼다. 챕터 6은 설계 당시 없던 칸이고,
2단계 웹 검증에서 실무 빈출 용어를 추가 발굴한 결과다(챕터 5는 종합이라 전용 용어가 없다).

각 용어 카드의 필드 (프롬프트 §7의 4필드 + 색인용 3개):
```js
{ term, en, chapter, analogy /*한 줄 비유*/, explain /*3줄*/, example /*실무 예시*/,
  myth /*자주 하는 오해*/, source }
```
확인 시점은 카드마다 넣지 않고 파일 하나에 `CHECKED_AT = '2026-08'` 로 두고 화면에 표시한다.
`source` 가 없으면 링크를 지어내는 대신 **"공식 정의가 있는 용어가 아니다"** 라고 화면에 밝힌다
(현재는 60개 전부 출처가 있다).

---

## 8. 저장 스키마

```js
// localStorage 키 하나만 쓴다: 'ai-course-v1'
{
  version: 1,
  cleared: { 'where-to-work': { grade: 'pass', score: 90, at: '2026-08-17' } },
  unlockedTerms: ['토큰', '컨텍스트 윈도우'],
  lastNode: 'where-to-work',
  settings: {}
}
```

- **`rank` 는 저장하지 않는다.** `cleared` 로 매번 계산한다 — 랭크 규칙을 고쳐도 옛 값이 남지 않는다.
- **`isCleared()` = 통과.** 반려(`fail`)도 기록은 되지만 **클리어로 세지 않는다.**
  진행도 막대·랭크·코스맵 완료 표시·게임 16의 심화 부품 해금이 전부 이 함수를 본다.
  "한 번이라도 해봤는가"는 `isTried()` 로 따로 본다. 코스맵은 반려한 노드를 ↻ 로 표시하고 다시 열어 둔다.
- 같은 게임을 다시 하면 **점수가 오를 때만** 덮어쓴다. 잘한 기록을 나중 판이 깎지 않는다.
- `version` 불일치 시 조용히 초기화 (마이그레이션 코드를 만들지 않는다)
- **"처음부터" 버튼**: 이 키 하나만 지우면 완전 초기화. 브라우저 기본 대화상자 대신
  그 자리에서 "정말 지운다 / 취소" 버튼이 나온다.
- localStorage 를 못 쓰는 환경(사생활 보호 모드)에서도 게임은 그대로 돌아간다. 저장만 안 될 뿐이다.
- 수집·전송·로그인 없음. 이 키 외에 아무것도 저장하지 않는다

---

## 9. 접근성 · 입력

| 항목 | 규칙 |
|---|---|
| 조작 대상 | 최소 44×44px (`--tap-min`) |
| 키보드 | 전 게임 Tab + Enter로 완주 가능. 조작 대상은 전부 `<button>` 이다 |
| 포커스 | 링 제거 금지. `--mark` 색 2px 외곽선 + 2px 여백 |
| 대비 | 본문 15.9:1 · 보조 5.4:1 · 상태색 5.7~8.5:1 — 전부 4.5:1 이상 (실측값은 `assets/tokens.css` 주석) |
| 스크린리더 | 카드·통에 `aria-label`, 판정·진행 상황은 `#live`(`role="status"`)에 문장으로 넣는다 |
| 모션 | `prefers-reduced-motion` 존중 — 도장은 애니메이션 없이 즉시 표시 |
| 색각 | 색 + 문구 + 형태 3중 표기. 색만으로 구분하지 않음 |
| 확대 | 200% 확대에서 가로 스크롤 없음 |

---

## 10. 파일 구조

프롬프트 §9를 따르되 몇 개를 추가했다. 아래가 **실제로 만들어진 것**이다.

```
/
├── index.html
├── .nojekyll
├── start.cmd            ← 추가: 로컬 확인용 정적 서버
├── README.md
├── CLAUDE.md            ← 추가: 다음 세션용 규칙
├── PROGRESS.md          ← 추가: 되는 것 / 다음 할 일
├── GAME_SPEC.md         ← 이 문서
├── TERMS.md             콘텐츠 원본
├── assets/
│   ├── tokens.css       §3 디자인 토큰만
│   ├── styles.css · game.css · codex.css
│   └── vendor/          anime.esm.min.js · anime.LICENSE.md  ← CDN 대신 넣어 둔 것
└── src/
    ├── main.js
    ├── core/     state.js · router.js · sim.js · motion.js · text.js · ko.js
    ├── shell/    topbar.js · coursemap.js · debrief.js · codex.js · toolbook.js · report.js
    ├── engines/  base.js · sort.js · spot.js · build.js · timeline.js · preview.js
    ├── games/    01~17 (파일 하나 = 미니게임 하나) + index.js (manifest)
    └── data/     terms.js (+ terms-work.js · terms-extra.js) · products.js · tools.js
```

설계에 있었지만 **만들지 않은 것**: `core/rules.js`(→ `core/sim.js` + 게임별 `simulate()`, §6).

설계에 없었지만 **필요해서 만든 것**:

| 파일 | 왜 |
|---|---|
| `core/sim.js` | 실행 로그와 판정을 같은 자리에서 만들어야 "과정을 보여주는" 게임이 된다 |
| `core/motion.js` | anime.js 를 부르는 유일한 파일. 프리셋을 한 곳에 모아 안전망 규칙을 강제한다 (§3) |
| `core/text.js` | 콘텐츠는 강조를 `**이렇게**` 로 적는다. `strong()` 이 **이스케이프를 먼저 하고** `<b>` 로 바꾼다 — 콘텐츠가 태그를 주입할 수 없다 |
| `core/ko.js` | 조사 처리. "채팅다 / 프로젝트이다" 가 화면에 나오면 그 자체로 결함이다 |
| `engines/base.js` | 엔진 5종이 같은 부품(`runner` · `Bin` · `actions`)을 쓰게 한다 |
| `shell/report.js` | 결과 카드 (프롬프트 §3) |
| `data/terms-work.js` · `terms-extra.js` | 용어가 60개로 늘어 `terms.js` 하나가 500줄을 넘겼다. `terms.js` 가 둘을 합쳐 내보낸다 |

**규칙 (games_zombie에서 검증된 것)**
1. 파일 하나가 **500줄**을 넘으면 분리한다
2. 엔진에 콘텐츠(문항·정답·문구)를 넣지 않는다. 전부 `games/` · `data/`
3. `core/`와 `engines/`는 특정 미니게임을 몰라야 한다
4. 미니게임 추가 = `games/` 파일 하나 추가 + `games/index.js` 의 `manifest` 에 한 줄(`ready: true`).
   코스맵·상단바·결과 카드는 전부 `manifest` 만 보므로 고칠 필요가 없다

---

## 11. 2단계에서 채운 것 (완료)

**검증 안 된 제품 정보를 지어내지 않는다**(프롬프트 §11). 아래는 전부 채워졌다.

| 대상 | 검증 방식 | 결과 파일 | 결과 |
|---|---|---|---|
| 미니게임 5·6·7·8의 카드 문항 | Claude/OpenAI **공식 문서 전수 확인** | `data/products.js` | 완료 |
| 용어 정의 | 웹 검색 + 출처 링크 | `TERMS.md` → `data/terms.js` | **60개** (전부 출처 있음) |
| 도구 도감 카테고리 | 카테고리 틀 먼저 확정 | `data/tools.js` | **11종** |
| 도구 카드 | 서비스 중단·통합 여부 + 공식 링크. **못 찾으면 넣지 않고 사유 기록** | `data/tools.js` | **35장** |

확인 시점은 게임 파일마다 `checkedAt: '2026-08'`, 데이터 파일은 `CHECKED_AT` 로 두고 화면에 찍는다.
도감 상단 배너("확인하고 쓰세요") 고정 — 스크롤해도 사라지지 않는다.
도구 카드는 장점만 적지 않는다. `watchOut`(주의점)이 비면 그 카드 자체가 워크슬롭이다.

---

## 12. 3단계 완료 조건

- [x] 서버 없이 정적 파일만으로 실행 — 빌드 도구 없음, 의존성 설치 없음
- [x] 코스맵 → 미니게임 → 판정 도장 → 해설 → 용어 수집 → 코스맵 복귀 **한 바퀴**
- [x] 진행도 저장 · 새로고침 후 이어짐 · "처음부터" 초기화
- [x] 360~390px 화면에서 손가락 조작 정상 (실측은 아래 §검증)
- [x] 콘솔 에러 0 · **외부 네트워크 요청 0건** (개발자도구로 실측)

**`index.html` 더블클릭은 안 된다.** ES 모듈 + `file://` 을 브라우저가 막는다.
설계 당시의 조건을 그대로 만족시키려면 모듈을 포기해야 해서, 조건 쪽을 바꿨다.
대신 그 상황을 감지해 **무엇을 하면 되는지 화면으로 안내한다**(`start.cmd` 실행 또는 배포 주소).
배포 주소로 열면 클릭 한 번으로 들어간다 — 실제 사용자는 전부 그쪽이다.

---

## 검증

17판 **전부**를 실제 브라우저에서 **정답 경로와 오답 경로 두 번씩** 끝까지 돌렸다.
게임을 만들 때마다 각각 확인한 것 말고, 17개를 한 번에 통과시킨 기록이다.

| 항목 | 결과 |
|---|---|
| 미니게임 완주 | 17 / 17 — 통과 경로 + 반려 경로 각 1회 |
| 판정 표시 | 승인·조건부 도장 / 반려 도장 + 실패 사유가 화면에 뜸 |
| 콘솔 에러 | **0건** |
| 외부 네트워크 요청 | **0건** (CDN·웹폰트·아이콘·애널리틱스·API 전부 없음) |
| 390px 가로 스크롤 | **없음** |
| 조작 대상 크기 | 전부 44px 이상 |

`_test.js`(자동 플레이 하네스, `.gitignore` 로 배포본에서 빠진다)가 각 게임의 `simulate()` 로
이기는 세팅과 지는 세팅을 먼저 찾고, 그 다음 **실제 버튼을 눌러** 끝까지 돌린다.
`simulate()` 를 직접 호출해 점수만 확인하는 방식은 쓰지 않았다 — 그건 화면이 도는 증거가 아니다.
